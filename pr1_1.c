/*
Program to find area and volume of a sphere.
Practical No 2, 2nd
*/

#include <stdio.h>

void main()
{
    int no1, no2, sum;
    printf("Enter the numbers: ");
    scanf("%d %d", &no1, &no2);
    sum=no1+no2;
    printf("SUM = %d", sum);
}

/*
MY WAY TO DO IT:-
#include <stdio.h>

int main()
{
    int no1, no2;
    printf("Enter the numbers: ");
    scanf("%d %d", &no1, &no2);
    printf("SUM = %d", no1 + no2);

    return 0;
}
*/

